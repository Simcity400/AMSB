amsb_italian_fleet_mediterranean_lock = {
	name = "Italian Fleet Leaving the Mediterranean"
	group = "HMM Rules"
	icon = "GFX_decision_generic_naval"

	default = {
		name = restricted
		text = "Restricted"
		desc = "Italian surface ships cannot pass Gibraltar or Bab el-Mandeb, even when Italy controls every prerequisite. Italian submarines and all other countries remain unaffected."
	}

	option = {
		name = unrestricted
		text = "Unrestricted"
		desc = "Italian ships use the same strait access rules as every other country."
	}
}
