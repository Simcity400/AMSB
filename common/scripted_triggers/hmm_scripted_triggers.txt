is_playable_country = {
	OR = {
		tag = GER
		tag = ITA
		tag = HUN
		tag = ROM
		tag = SPR
		tag = BUL
		tag = JAP
		tag = SIA # restored playable (main lobby mode only - SIA doesn't exist in other modes) 2026-07-23
		# tag = MAN
		tag = FIN
		tag = ENG
		tag = FRA
		tag = CAN
		tag = AST
		# tag = BRA
		tag = MEX
		# tag = NZL
		tag = USA
		tag = RAJ
		tag = SAF
		tag = SOV
		tag = DEN # restored playable (SPTv2 tree) 2026-07-23
		tag = PHI # restored playable (main lobby mode only) 2026-07-23

	}
}

allies = {
	OR = {
		tag = ENG
		tag = FRA
		tag = CAN
		tag = AST
		tag = BRA
		tag = MEX
		tag = NZL
		tag = USA
		tag = RAJ
		tag = SAF
		tag = IRE
		tag = SOV
		tag = NOR
		tag = HOL
		tag = BEL
		tag = LUX
		tag = POL
		tag = DEN
		tag = PHI
		tag = YUG
		tag = GRE
		tag = IRE
	}
}


axis = {
	OR = {
		tag = GER
		tag = ITA
		tag = HUN
		tag = ROM
		tag = SPR
		tag = BUL
		tag = JAP
		tag = SIA
		tag = MAN
		tag = FIN
		tag = RKB
		tag = RKN
		tag = RKG
		tag = RKU
		tag = RKO
		tag = RKK
		tag = RKT
		tag = RKM
		tag = RKL
		tag = GEN
		tag = RKH
		tag = RKI
		tag = RKC
		tag = RGB
		tag = RKA
		tag = RNA
		tag = RUS
		tag = RCO
		tag = RAN
		tag = RKV
		tag = RHD
		tag = RAR
		tag = ROA
		tag = RAA
		tag = EFR
	}
}
# These four lists cripple AI-only minors so they stay passive. Every tag here must be
# absent from is_playable_country above: a country in one of these lists gets ai_war /
# ai_annex + ai_tradelaw and country_lock_all_division_template at on_startup, and those
# ideas have removal_cost = -1 with no is_ai gate - so a HUMAN who picks the country is
# stuck with -100% factory output, +10000% consumer goods, unchangeable trade laws and
# locked division templates, permanently, with no way to remove any of it.
ai_manpower = {
	OR = {
		tag = BEL
		tag = NOR
		tag = LUX
		tag = HOL
		tag = POL
		tag = YUG
		tag = CHI
		tag = GRE
		# DEN removed 2026-07-26: Denmark was made playable on 2026-07-23 but left in this
		# list, so anyone who picked it started permanently crippled.
		tag = ALB
		tag = EFR
		tag = IRE

	}
}
ai_annex = {
	OR = {
		tag = LIT
		tag = LAT
		tag = EST
		tag = AUS
		tag = CZE
		tag = POR
		tag = BRM
		# PHI removed 2026-07-26: same oversight as DEN - made playable 2026-07-23 and left
		# in this list, so a human Philippines started permanently crippled.
	}
}
ai_rk = {
	OR = {
		tag = RKB
		tag = RKN
		tag = RKG
		tag = RKU
		tag = RKO
		tag = RKK
		tag = RKT
		tag = RKM
		tag = RKL
		tag = GEN
		tag = RKH
		tag = RKI
		tag = RKC
		tag = RGB
		tag = RKA
		tag = RNA
		tag = RUS
		tag = RCO
		tag = RAN
		tag = RKV
		tag = RHD
		tag = RAR
		tag = ROA
		tag = RAA
	}
}
ai_black = {
	OR = {
		tag = WST
		tag = CHO
		tag = SWE
		tag = PER
		tag = TUR
		tag = SWI
		tag = VEN
	}
}

DIPLOMACY_PEACE_PROPOSAL_ENABLE_TRIGGER = {
	always = no
}

is_vichy_france = {
	original_tag = EFR
}
