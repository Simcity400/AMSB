# GENERATED-style host tool law changer. Sets economy/trade/mobilization laws on the MARKED state's owner.
# Mark a state, click the "Laws" host-tool button (sets ht_law_mode), pick a law here.
host_tool_law_category = {
	# ---- Economy ----
	ht_law_undisturbed_isolation = {
		icon = generic_research
		cost = 0
		fire_only_once = no
		visible = {
			event_target:ht_focus_state = { owner = { exists = yes } }
			has_country_flag = ht_law_mode
			event_target:ht_focus_state = { owner = { NOT = { has_idea = undisturbed_isolation } } }
		}
		complete_effect = {
			if = {
				limit = { event_target:ht_focus_state = { owner = { exists = yes } } }
			event_target:ht_focus_state = { owner = { add_ideas = undisturbed_isolation } }
			}
		}
	}
	ht_law_isolation = {
		icon = generic_research
		cost = 0
		fire_only_once = no
		visible = {
			event_target:ht_focus_state = { owner = { exists = yes } }
			has_country_flag = ht_law_mode
			event_target:ht_focus_state = { owner = { NOT = { has_idea = isolation } } }
		}
		complete_effect = {
			if = {
				limit = { event_target:ht_focus_state = { owner = { exists = yes } } }
			event_target:ht_focus_state = { owner = { add_ideas = isolation } }
			}
		}
	}
	ht_law_civilian_economy = {
		icon = generic_research
		cost = 0
		fire_only_once = no
		visible = {
			event_target:ht_focus_state = { owner = { exists = yes } }
			has_country_flag = ht_law_mode
			event_target:ht_focus_state = { owner = { NOT = { has_idea = civilian_economy } } }
		}
		complete_effect = {
			if = {
				limit = { event_target:ht_focus_state = { owner = { exists = yes } } }
			event_target:ht_focus_state = { owner = { add_ideas = civilian_economy } }
			}
		}
	}
	ht_law_low_economic_mobilisation = {
		icon = generic_research
		cost = 0
		fire_only_once = no
		visible = {
			event_target:ht_focus_state = { owner = { exists = yes } }
			has_country_flag = ht_law_mode
			event_target:ht_focus_state = { owner = { NOT = { has_idea = low_economic_mobilisation } } }
		}
		complete_effect = {
			if = {
				limit = { event_target:ht_focus_state = { owner = { exists = yes } } }
			event_target:ht_focus_state = { owner = { add_ideas = low_economic_mobilisation } }
			}
		}
	}
	ht_law_partial_economic_mobilisation = {
		icon = generic_research
		cost = 0
		fire_only_once = no
		visible = {
			event_target:ht_focus_state = { owner = { exists = yes } }
			has_country_flag = ht_law_mode
			event_target:ht_focus_state = { owner = { NOT = { has_idea = partial_economic_mobilisation } } }
		}
		complete_effect = {
			if = {
				limit = { event_target:ht_focus_state = { owner = { exists = yes } } }
			event_target:ht_focus_state = { owner = { add_ideas = partial_economic_mobilisation } }
			}
		}
	}
	ht_law_war_economy = {
		icon = generic_research
		cost = 0
		fire_only_once = no
		visible = {
			event_target:ht_focus_state = { owner = { exists = yes } }
			has_country_flag = ht_law_mode
			event_target:ht_focus_state = { owner = { NOT = { has_idea = war_economy } } }
		}
		complete_effect = {
			if = {
				limit = { event_target:ht_focus_state = { owner = { exists = yes } } }
			event_target:ht_focus_state = { owner = { add_ideas = war_economy } }
			}
		}
	}
	ht_law_tot_economic_mobilisation = {
		icon = generic_research
		cost = 0
		fire_only_once = no
		visible = {
			event_target:ht_focus_state = { owner = { exists = yes } }
			has_country_flag = ht_law_mode
			event_target:ht_focus_state = { owner = { NOT = { has_idea = tot_economic_mobilisation } } }
		}
		complete_effect = {
			if = {
				limit = { event_target:ht_focus_state = { owner = { exists = yes } } }
			event_target:ht_focus_state = { owner = { add_ideas = tot_economic_mobilisation } }
			}
		}
	}
	ht_law_totaler_krieg_economy = {
		icon = generic_research
		cost = 0
		fire_only_once = no
		visible = {
			event_target:ht_focus_state = { owner = { exists = yes } }
			has_country_flag = ht_law_mode
			event_target:ht_focus_state = { owner = { NOT = { has_idea = totaler_krieg_economy } } }
		}
		complete_effect = {
			if = {
				limit = { event_target:ht_focus_state = { owner = { exists = yes } } }
			event_target:ht_focus_state = { owner = { add_ideas = totaler_krieg_economy } }
			}
		}
	}
	ht_law_national_defense_state = {
		icon = generic_research
		cost = 0
		fire_only_once = no
		visible = {
			event_target:ht_focus_state = { owner = { exists = yes } }
			has_country_flag = ht_law_mode
			event_target:ht_focus_state = { owner = { NOT = { has_idea = national_defense_state } } }
		}
		complete_effect = {
			if = {
				limit = { event_target:ht_focus_state = { owner = { exists = yes } } }
			event_target:ht_focus_state = { owner = { add_ideas = national_defense_state } }
			}
		}
	}
	ht_law_new_economic_policy = {
		icon = generic_research
		cost = 0
		fire_only_once = no
		visible = {
			event_target:ht_focus_state = { owner = { exists = yes } }
			has_country_flag = ht_law_mode
			event_target:ht_focus_state = { owner = { NOT = { has_idea = new_economic_policy } } }
		}
		complete_effect = {
			if = {
				limit = { event_target:ht_focus_state = { owner = { exists = yes } } }
			event_target:ht_focus_state = { owner = { add_ideas = new_economic_policy } }
			}
		}
	}
	ht_law_new_economic_policy_2 = {
		icon = generic_research
		cost = 0
		fire_only_once = no
		visible = {
			event_target:ht_focus_state = { owner = { exists = yes } }
			has_country_flag = ht_law_mode
			event_target:ht_focus_state = { owner = { NOT = { has_idea = new_economic_policy_2 } } }
		}
		complete_effect = {
			if = {
				limit = { event_target:ht_focus_state = { owner = { exists = yes } } }
			event_target:ht_focus_state = { owner = { add_ideas = new_economic_policy_2 } }
			}
		}
	}
	ht_law_capital_investment_model = {
		icon = generic_research
		cost = 0
		fire_only_once = no
		visible = {
			event_target:ht_focus_state = { owner = { exists = yes } }
			has_country_flag = ht_law_mode
			event_target:ht_focus_state = { owner = { NOT = { has_idea = capital_investment_model } } }
		}
		complete_effect = {
			if = {
				limit = { event_target:ht_focus_state = { owner = { exists = yes } } }
			event_target:ht_focus_state = { owner = { add_ideas = capital_investment_model } }
			}
		}
	}
	# ---- Trade ----
	ht_law_uncontrolled_exports = {
		icon = generic_research
		cost = 0
		fire_only_once = no
		visible = {
			event_target:ht_focus_state = { owner = { exists = yes } }
			has_country_flag = ht_law_mode
			event_target:ht_focus_state = { owner = { NOT = { has_idea = uncontrolled_exports } } }
		}
		complete_effect = {
			if = {
				limit = { event_target:ht_focus_state = { owner = { exists = yes } } }
			event_target:ht_focus_state = { owner = { add_ideas = uncontrolled_exports } }
			}
		}
	}
	ht_law_free_trade = {
		icon = generic_research
		cost = 0
		fire_only_once = no
		visible = {
			event_target:ht_focus_state = { owner = { exists = yes } }
			has_country_flag = ht_law_mode
			event_target:ht_focus_state = { owner = { NOT = { has_idea = free_trade } } }
		}
		complete_effect = {
			if = {
				limit = { event_target:ht_focus_state = { owner = { exists = yes } } }
			event_target:ht_focus_state = { owner = { add_ideas = free_trade } }
			}
		}
	}
	ht_law_export_focus = {
		icon = generic_research
		cost = 0
		fire_only_once = no
		visible = {
			event_target:ht_focus_state = { owner = { exists = yes } }
			has_country_flag = ht_law_mode
			event_target:ht_focus_state = { owner = { NOT = { has_idea = export_focus } } }
		}
		complete_effect = {
			if = {
				limit = { event_target:ht_focus_state = { owner = { exists = yes } } }
			event_target:ht_focus_state = { owner = { add_ideas = export_focus } }
			}
		}
	}
	ht_law_limited_exports = {
		icon = generic_research
		cost = 0
		fire_only_once = no
		visible = {
			event_target:ht_focus_state = { owner = { exists = yes } }
			has_country_flag = ht_law_mode
			event_target:ht_focus_state = { owner = { NOT = { has_idea = limited_exports } } }
		}
		complete_effect = {
			if = {
				limit = { event_target:ht_focus_state = { owner = { exists = yes } } }
			event_target:ht_focus_state = { owner = { add_ideas = limited_exports } }
			}
		}
	}
	ht_law_autarkic_economy = {
		icon = generic_research
		cost = 0
		fire_only_once = no
		visible = {
			event_target:ht_focus_state = { owner = { exists = yes } }
			has_country_flag = ht_law_mode
			event_target:ht_focus_state = { owner = { NOT = { has_idea = autarkic_economy } } }
		}
		complete_effect = {
			if = {
				limit = { event_target:ht_focus_state = { owner = { exists = yes } } }
			event_target:ht_focus_state = { owner = { add_ideas = autarkic_economy } }
			}
		}
	}
	ht_law_autarkic_economy_improved = {
		icon = generic_research
		cost = 0
		fire_only_once = no
		visible = {
			event_target:ht_focus_state = { owner = { exists = yes } }
			has_country_flag = ht_law_mode
			event_target:ht_focus_state = { owner = { NOT = { has_idea = autarkic_economy_improved } } }
		}
		complete_effect = {
			if = {
				limit = { event_target:ht_focus_state = { owner = { exists = yes } } }
			event_target:ht_focus_state = { owner = { add_ideas = autarkic_economy_improved } }
			}
		}
	}
	ht_law_closed_economy = {
		icon = generic_research
		cost = 0
		fire_only_once = no
		visible = {
			event_target:ht_focus_state = { owner = { exists = yes } }
			has_country_flag = ht_law_mode
			event_target:ht_focus_state = { owner = { NOT = { has_idea = closed_economy } } }
		}
		complete_effect = {
			if = {
				limit = { event_target:ht_focus_state = { owner = { exists = yes } } }
			event_target:ht_focus_state = { owner = { add_ideas = closed_economy } }
			}
		}
	}
	ht_law_special_economic_zones = {
		icon = generic_research
		cost = 0
		fire_only_once = no
		visible = {
			event_target:ht_focus_state = { owner = { exists = yes } }
			has_country_flag = ht_law_mode
			event_target:ht_focus_state = { owner = { NOT = { has_idea = special_economic_zones } } }
		}
		complete_effect = {
			if = {
				limit = { event_target:ht_focus_state = { owner = { exists = yes } } }
			event_target:ht_focus_state = { owner = { add_ideas = special_economic_zones } }
			}
		}
	}
	# ---- Mobilization ----
	ht_law_disarmed_nation = {
		icon = generic_research
		cost = 0
		fire_only_once = no
		visible = {
			event_target:ht_focus_state = { owner = { exists = yes } }
			has_country_flag = ht_law_mode
			event_target:ht_focus_state = { owner = { NOT = { has_idea = disarmed_nation } } }
		}
		complete_effect = {
			if = {
				limit = { event_target:ht_focus_state = { owner = { exists = yes } } }
			event_target:ht_focus_state = { owner = { add_ideas = disarmed_nation } }
			}
		}
	}
	ht_law_volunteer_only = {
		icon = generic_research
		cost = 0
		fire_only_once = no
		visible = {
			event_target:ht_focus_state = { owner = { exists = yes } }
			has_country_flag = ht_law_mode
			event_target:ht_focus_state = { owner = { NOT = { has_idea = volunteer_only } } }
		}
		complete_effect = {
			if = {
				limit = { event_target:ht_focus_state = { owner = { exists = yes } } }
			event_target:ht_focus_state = { owner = { add_ideas = volunteer_only } }
			}
		}
	}
	ht_law_limited_conscription = {
		icon = generic_research
		cost = 0
		fire_only_once = no
		visible = {
			event_target:ht_focus_state = { owner = { exists = yes } }
			has_country_flag = ht_law_mode
			event_target:ht_focus_state = { owner = { NOT = { has_idea = limited_conscription } } }
		}
		complete_effect = {
			if = {
				limit = { event_target:ht_focus_state = { owner = { exists = yes } } }
			event_target:ht_focus_state = { owner = { add_ideas = limited_conscription } }
			}
		}
	}
	ht_law_extensive_conscription = {
		icon = generic_research
		cost = 0
		fire_only_once = no
		visible = {
			event_target:ht_focus_state = { owner = { exists = yes } }
			has_country_flag = ht_law_mode
			event_target:ht_focus_state = { owner = { NOT = { has_idea = extensive_conscription } } }
		}
		complete_effect = {
			if = {
				limit = { event_target:ht_focus_state = { owner = { exists = yes } } }
			event_target:ht_focus_state = { owner = { add_ideas = extensive_conscription } }
			}
		}
	}
	ht_law_service_by_requirement = {
		icon = generic_research
		cost = 0
		fire_only_once = no
		visible = {
			event_target:ht_focus_state = { owner = { exists = yes } }
			has_country_flag = ht_law_mode
			event_target:ht_focus_state = { owner = { NOT = { has_idea = service_by_requirement } } }
		}
		complete_effect = {
			if = {
				limit = { event_target:ht_focus_state = { owner = { exists = yes } } }
			event_target:ht_focus_state = { owner = { add_ideas = service_by_requirement } }
			}
		}
	}
	ht_law_all_adults_serve = {
		icon = generic_research
		cost = 0
		fire_only_once = no
		visible = {
			event_target:ht_focus_state = { owner = { exists = yes } }
			has_country_flag = ht_law_mode
			event_target:ht_focus_state = { owner = { NOT = { has_idea = all_adults_serve } } }
		}
		complete_effect = {
			if = {
				limit = { event_target:ht_focus_state = { owner = { exists = yes } } }
			event_target:ht_focus_state = { owner = { add_ideas = all_adults_serve } }
			}
		}
	}
	ht_law_scraping_the_barrel = {
		icon = generic_research
		cost = 0
		fire_only_once = no
		visible = {
			event_target:ht_focus_state = { owner = { exists = yes } }
			has_country_flag = ht_law_mode
			event_target:ht_focus_state = { owner = { NOT = { has_idea = scraping_the_barrel } } }
		}
		complete_effect = {
			if = {
				limit = { event_target:ht_focus_state = { owner = { exists = yes } } }
			event_target:ht_focus_state = { owner = { add_ideas = scraping_the_barrel } }
			}
		}
	}
}
